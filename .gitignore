node_modules/
dist/
.env
.env.local

# Runtime state: your config, the user directory, and any media you drop in.
data/
!data/.gitkeep

*.log
.DS_Store

# Rust build output for the desktop panel. Regenerated by `npm run panel`;
# 6.3 GB of it here, which is normal for a debug + release Tauri build.
target/

# TypeScript incremental build cache.
*.tsbuildinfo
